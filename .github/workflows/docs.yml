# Docs deployment is handled by deploy.yml (SFTP to UFRGS server).
# This file is kept as a placeholder to avoid ODK template drift.
name: Docs (disabled)

on:
  workflow_dispatch:

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Docs are deployed via deploy.yml on push to main."
